version: 0.2
phases:

  install:
     runtime-versions:
      java: openjdk8
  build:
    commands:
      - mvn test
   - mvn clean install
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - target/*.jar
    - scripts/*.sh
    - appspec.yml
  discard-paths: yes
